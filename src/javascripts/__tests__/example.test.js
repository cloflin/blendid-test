// See http://chaijs.com/ for assertion style docs

describe('something', () => {
  it('does some stuff', () => {
    assert.isTrue(true)
  })
})

describe('Config Tests', () => {
  it('passes this canary test', () => {
    assert.isTrue(true);
  });
});
